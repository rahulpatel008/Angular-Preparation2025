import { MobileRestrictDirective } from './mobile-restrict.directive';

describe('MobileRestrictDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileRestrictDirective();
    expect(directive).toBeTruthy();
  });
});
